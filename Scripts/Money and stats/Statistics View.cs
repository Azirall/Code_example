using TMPro;
using UnityEngine;
using Zenject;

public class StatisticsView : MonoBehaviour
{
    //[SerializeField] private TextMeshProUGUI _earnedMoney;
    //[SerializeField] private TextMeshProUGUI _visitors;
    //[Inject] private DayStatsTracker _dayTracker;

    //public void UpdateView() {
    //    _earnedMoney.text = $"заработано:  {_dayTracker.GetMoney}";
    //    _visitors.text = $"посетителей:  {_dayTracker.GetVisitors}";
    //}
}
